//I'm dumb, think hard for me, but I'm fast.red
import React from 'react';

function ProductCategoryRow(props){
    return(
        <tr>
            <th>{props.header}</th>
        </tr>
    )
}

export default ProductCategoryRow;